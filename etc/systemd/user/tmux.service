[Unit]
Description=tmux server (persistent workspace, restored by tmux-resurrect)
Documentation=man:tmux(1)

[Service]
Type=forking
ExecStart=/usr/bin/tmux new-session -d -s main
ExecStop=/usr/bin/tmux kill-server
KillMode=process
Restart=on-failure

[Install]
WantedBy=default.target
